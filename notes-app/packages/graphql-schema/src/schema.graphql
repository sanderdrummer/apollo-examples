"""
ISO-8601 DateTime
"""
scalar DateTime

type Note {
  id: ID!
  text: String!
  created: DateTime!
}
type DateRange {
  from: DateTime
  to: DateTime
}
type QueryNotesInput {
  created: DateRange
}

type Query {
  notes(input: QueryNotesInput): [Note!]!
}
